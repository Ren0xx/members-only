<!DOCTYPE html>
<html data-theme="winter">
    <head>
        <title><%= title %></title>
        <link rel="stylesheet" href="/stylesheets/style.css" />
        <link
            href="https://cdn.jsdelivr.net/npm/daisyui@2.51.3/dist/full.css"
            rel="stylesheet"
            type="text/css" />
        <script src="https://cdn.tailwindcss.com?plugins=forms,typography,aspect-ratio,line-clamp"></script>
    </head>
    <body class="flex flex-col gap-4 items-center justify-center h-screen">
        <a href="/" class="absolute top-8 left-8 link">Go back</a>
        <form
            class="form-control w-full max-w-xs gap-8 px-8 py-8"
            action=""
            method="POST">
            <div>
                <label class="label my-4">
                    <span class="label-text text-lg">Secret code</span>
                </label>
                <input
                    type="password"
                    placeholder="Type code here"
                    class="input w-full max-w-xs rounded-lg"
                    name="secretcode" />
            </div>
            <% if (errors.length > 0) { %>
            <ul class="text-error">
                <% errors.forEach(function(error) { %>
                <li><%= error.msg %></li>
                <% }); %>
            </ul>
            <% } %>
            <button class="btn bg-blue-600 btn-sm" type="submit">Submit</button>
        </form>
    </body>
</html>
